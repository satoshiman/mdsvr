# Sidebar OrderBy Feature

## Settings Schema Update

File: `src/settings/schema.ts` (lines 45-55)

```typescript
sidebar: z
  .object({
    enabled: z.boolean().default(true),
    autoGenerate: z.boolean().default(true),
    showFileCount: z.boolean().default(false),
    collapsible: z.boolean().default(true),
    defaultOpen: z.boolean().default(true),
    depth: z.number().default(2),
    docsOnly: z.boolean().default(false),
    // NEW: Order options
    orderBy: z.enum(["alphabetical", "prior-dir", "manual"]).default("alphabetical"),
    manualOrder: z.array(z.string()).optional(), // for "manual" mode
  })
  .default({}),
```

## NavItem Interface Update

File: `src/template/sidebar.ts` (lines 6-15)

```typescript
export interface NavItem {
  title: string;
  href: string;
  children?: NavItem[];
  active?: boolean;
  hasActiveChild?: boolean;
  type: "file" | "dir";
  order?: number;
  sortBy: string;
  manualOrder?: number; // NEW: for manual sorting
}
```

## Sort Logic Update

File: `src/template/sidebar.ts` (replace lines 211-216)

```typescript
// Sort based on orderBy setting
const orderBy = settings.navigation.sidebar.orderBy;
const manualOrderList = settings.navigation.sidebar.manualOrder || [];

return items.sort((a, b) => {
  switch (orderBy) {
    case "manual": {
      const aIndex = manualOrderList.indexOf(a.sortBy);
      const bIndex = manualOrderList.indexOf(b.sortBy);
      // Items in manualOrder list come first, in specified order
      // Items not in list are appended at end, sorted alphabetically
      if (aIndex !== -1 && bIndex !== -1) return aIndex - bIndex;
      if (aIndex !== -1) return -1;
      if (bIndex !== -1) return 1;
      return a.sortBy.localeCompare(b.sortBy);
    }

    case "prior-dir": {
      // Folder first, then file; same type sort by name
      if (a.type === "dir" && b.type !== "dir") return -1;
      if (a.type !== "dir" && b.type === "dir") return 1;
      return a.sortBy.localeCompare(b.sortBy);
    }

    case "alphabetical":
    default: {
      // Mixed: folder & file together, pure alphabetical
      return a.sortBy.localeCompare(b.sortBy);
    }
  }
});
```

## Usage Examples

### 1. Alphabetical (default) - Mixed

```json
{
  "navigation": {
    "sidebar": {
      "orderBy": "alphabetical"
    }
  }
}
```

Result: `api.md`, `assets/` (folder), `guide.md`, `styles/` (folder)

### 2. Prior Directory - Folders First

```json
{
  "navigation": {
    "sidebar": {
      "orderBy": "prior-dir"
    }
  }
}
```

Result: `assets/` (folder), `styles/` (folder), `api.md`, `guide.md`

### 3. Manual - Custom Order

```json
{
  "navigation": {
    "sidebar": {
      "orderBy": "manual",
      "manualOrder": [
        "getting-started",
        "core-concepts",
        "advanced",
        "api-reference"
      ]
    }
  }
}
```

Items in `manualOrder` appear first in specified order. Items not listed append at end alphabetically.

## Migration Note

- **Current behavior** = `prior-dir` (folders first)
- **New default** = `alphabetical` (mixed)

To maintain existing behavior after update, explicitly set:

```json
{ "navigation": { "sidebar": { "orderBy": "prior-dir" } } }
```

---

## Directory Title Logic Change

**Current:** Directory title = `humanizeFilename(folderName)`

**New:** Directory title = `extractTitle(indexFile)` → fallback → `humanizeFilename(folderName)`

### New Function to Add

File: `src/template/sidebar.ts` (after `hasDirectoryIndex`, around line 87)

```typescript
async function getDirectoryTitle(
  dirPath: string,
  folderName: string,
  settings: Settings,
): Promise<string> {
  // Try to extract title from index file (README.md, index.md, etc.)
  for (const indexFile of settings.files.indexFiles) {
    const indexPath = path.join(dirPath, indexFile);
    try {
      await fs.access(indexPath);
      const title = await extractTitle(indexPath, settings);
      if (title && title !== "Home") {
        return title;
      }
    } catch {
      // Continue to next index file
    }
  }

  // Fallback to humanizeFilename
  return humanizeFilename(folderName);
}
```

### Update Directory Item Creation

File: `src/template/sidebar.ts` (line 181)

```typescript
// OLD:
title: humanizeFilename(entry.name),

// NEW:
title: await getDirectoryTitle(fullPath, entry.name, settings),
```

### Examples

| Folder                | README.md title   | Old Display          | New Display       |
| --------------------- | ----------------- | -------------------- | ----------------- |
| `1.-getting-started/` | "Getting Started" | "1. Getting started" | "Getting Started" |
| `api-reference/`      | `# API Reference` | "Api reference"      | "API Reference"   |
| `core-concepts/`      | (no README)       | "Core concepts"      | "Core concepts"   |
