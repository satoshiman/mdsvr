# 🚀 Code Agent Guide: Export Split

Hướng dẫn chi tiết để Code Agent thực hiện tính năng:

**Tách export** thành 3 flag riêng:

- `-eo` / `--export-og` – chỉ gen OG images (có skip existing)
- `-es` / `--export-static` – chỉ export static HTML
- `-e` / `--export` – export cả 2 (static + OG)

---

## 📋 Tổng quan thay đổi

| Flag          | Hành vi                                                      |
| ------------- | ------------------------------------------------------------ |
| `mdsvr . -eo` | Gen OG images cho các page chưa có (skip existing)           |
| `mdsvr . -es` | Export static HTML sang `_html/public/` (không gen OG)       |
| `mdsvr . -e`  | Export static HTML **và** gen OG (tương đương `-es` + `-eo`) |

---

## 🎯 Task 1: Tách export flags trong `src/cli.ts`

### Thay đổi `ParsedArgs` interface

**Từ:**

```typescript
interface ParsedArgs {
  // ...
  export: string | null;
}
```

**Thành:**

```typescript
interface ParsedArgs {
  // ...
  export: string | null; // -e / --export (cả 2)
  exportOg: string | null; // -eo / --export-og (chỉ OG)
  exportStatic: string | null; // -es / --export-static (chỉ static HTML)
}
```

### Thêm default values trong `parseArgs()`

```typescript
const args: ParsedArgs = {
  // ...existing...
  export: null,
  exportOg: null,
  exportStatic: null,
};
```

### Thêm parsing logic (thêm vào trong vòng lặp `for` của `parseArgs`, sau block `--export`):

```typescript
    } else if (arg === "--export-og" || arg === "-eo") {
      const val = argv[++i];
      if (val && !val.startsWith("-")) {
        args.exportOg = val;
      } else {
        if (val) i--;
        args.exportOg = ""; // empty string = use default path
      }
    } else if (arg === "--export-static" || arg === "-es") {
      const val = argv[++i];
      if (val && !val.startsWith("-")) {
        args.exportStatic = val;
      } else {
        if (val) i--;
        args.exportStatic = ""; // empty string = use default path
      }
```

### Cập nhật `printHelp()`

```typescript
function printHelp(): void {
  console.log(`
Usage: mdsvr [dir] [options]

Options:
  [dir]                 Root directory to serve (default: .)
  -p, --port N          Port number (default: 1800)
  --host H              Bind address (default: localhost)
  -o, --open            Auto-open browser
  -s, --silent          Suppress console output
  -e,  --export [PATH]        Export static HTML + OG images
  -es, --export-static [PATH] Export static HTML only
  -eo, --export-og [PATH]     Export OG images only (skip existing)
  --init                Create a starter settings.json in [dir]
  --validate            Validate settings.json and exit
  --no-watch            Disable settings.json hot-reload
  -v, --version         Print version
  -h, --help            Print this help

Export paths default to: <dir>/_html/public

Examples:
  mdsvr ./docs
  mdsvr ./docs -e              # Export HTML + OG to ./_html/public
  mdsvr ./docs -es             # Export HTML only
  mdsvr ./docs -eo             # Export/update OG images only
  mdsvr ./docs -e ./output     # Export to custom directory
`);
}
```

### Thêm export handlers trong `main()`

Thêm **3 handlers** mới, thay thế handler `-e` hiện tại. Thứ tự ưu tiên:

1. `--export-og` (`-eo`)
2. `--export-static` (`-es`)
3. `--export` (`-e`) – chạy cả 2

```typescript
// Handle --export-og (only OG images, skip existing)
if (
  args.exportOg !== null ||
  process.argv.includes("--export-og") ||
  process.argv.includes("-eo")
) {
  try {
    const { loadSettings } = await import("./settings/index.js");
    const { exportOgImages } = await import("./generators/static-export.js");

    const settings = await loadSettings(absoluteDir);
    const outputDir =
      args.exportOg || path.join(absoluteDir, "_html", "public");
    await exportOgImages({
      rootDir: absoluteDir,
      outputDir,
      settings,
      silent: args.silent,
      skipExisting: true, // ← KEY: chỉ gen cái chưa có
    });

    if (!args.silent) {
      console.log(`\n  ✔ OG images exported to: ${outputDir}/public/og\n`);
    }
    process.exit(0);
  } catch (err) {
    console.error("Export OG error:", err instanceof Error ? err.message : err);
    process.exit(1);
  }
}

// Handle --export-static (only static HTML, no OG)
if (
  args.exportStatic !== null ||
  process.argv.includes("--export-static") ||
  process.argv.includes("-es")
) {
  try {
    const { loadSettings } = await import("./settings/index.js");
    const { exportStaticSite } = await import("./generators/static-export.js");

    const settings = await loadSettings(absoluteDir);
    const outputDir =
      args.exportStatic || path.join(absoluteDir, "_html", "public");
    await exportStaticSite({
      rootDir: absoluteDir,
      outputDir,
      settings,
      silent: args.silent,
      skipOg: true, // ← NEW option: bỏ qua bước gen OG
    });

    if (!args.silent) {
      console.log(`\n  ✔ Static site exported to: ${outputDir}\n`);
    }
    process.exit(0);
  } catch (err) {
    console.error("Export error:", err instanceof Error ? err.message : err);
    process.exit(1);
  }
}

// Handle --export (both static + OG) – giữ nguyên logic hiện tại
if (
  args.export !== null ||
  process.argv.includes("--export") ||
  process.argv.includes("-e")
) {
  // ... existing export logic, không đổi ...
}
```

---

## 🎯 Task 2: Tạo `exportOgImages()` trong `src/generators/static-export.ts`

### Thêm interface mới

```typescript
export interface ExportOgOptions {
  rootDir: string;
  outputDir: string;
  settings: Settings;
  silent?: boolean;
  skipExisting?: boolean; // nếu true: bỏ qua file đã tồn tại trong outputDir
}
```

### Thêm option `skipOg` vào `ExportOptions`

```typescript
export interface ExportOptions {
  rootDir: string;
  outputDir: string;
  settings: Settings;
  silent?: boolean;
  skipOg?: boolean; // NEW: bỏ qua bước generate OG
}
```

### Cập nhật `exportStaticSite()` để respect `skipOg`

Trong step 4 của `exportStaticSite()`:

```typescript
// 4. Generate OG images if enabled AND not skipped
if (settings.seo.og?.enabled && !options.skipOg) {
  // ... existing OG generation logic
}
```

### Implement `exportOgImages()`

```typescript
export async function exportOgImages(options: ExportOgOptions): Promise<void> {
  const {
    rootDir,
    outputDir,
    settings,
    silent = false,
    skipExisting = true,
  } = options;

  const absRootDir = path.resolve(rootDir);
  const absOutputDir = path.resolve(outputDir);
  const ogOutputDir = path.join(absOutputDir, "public", "og");

  await fs.mkdir(ogOutputDir, { recursive: true });

  if (!silent) {
    console.log(`\n  🖼️  Exporting OG images`);
    console.log(`  From: ${absRootDir}`);
    console.log(`  To:   ${ogOutputDir}\n`);
  }

  // Step 1: Collect all pages
  const pageInfos: PageInfo[] = [];
  await collectPageInfos(absRootDir, absRootDir, settings, pageInfos);

  // Step 2: Gen OG cho các page chưa có
  const format = settings.seo.og?.imageFormat || "jpg";
  const ogSettings = settings.seo.og;
  let generatedCount = 0;
  let skippedCount = 0;
  let failedCount = 0;

  for (const pageInfo of pageInfos) {
    const ogImagePath = getOgImagePath(
      pageInfo.urlPath,
      ogOutputDir,
      format,
      settings.generate.basePath,
    );

    // Skip nếu đã tồn tại
    if (skipExisting) {
      try {
        await fs.access(ogImagePath);
        skippedCount++;
        continue;
      } catch {
        // Chưa có, tiến hành gen
      }
    }

    const ogData: OgImageData = {
      title: pageInfo.title,
      description: pageInfo.description,
      siteName: settings.site.title,
      urlPath: pageInfo.urlPath,
      accentColor: settings.appearance.accentColor,
      fontFamily: ogSettings?.fontFamily || "Inter",
      backgroundColor: ogSettings?.colors?.background || "#0a0a0f",
      textColor: ogSettings?.colors?.text || "#ffffff",
    };

    const result = await generateOgImage(ogData, {
      outputPath: ogImagePath,
      format,
      quality: 85,
    });

    if (result.success) {
      generatedCount++;
      if (!silent) {
        const displayPath = path.relative(absOutputDir, ogImagePath);
        console.log(`  ✓ OG: ${displayPath}`);
      }
    } else {
      failedCount++;
      if (!silent) {
        console.log(`  ✗ OG failed: ${pageInfo.urlPath} (${result.error})`);
      }
    }
  }

  if (!silent) {
    console.log(
      `\n  ✔ OG: ${generatedCount} generated, ${skippedCount} skipped, ${failedCount} failed\n`,
    );
  }
}
```

### Helper `collectPageInfos()` (thu thập page list mà không render HTML)

```typescript
async function collectPageInfos(
  dirPath: string,
  rootDir: string,
  settings: Settings,
  pageInfos: PageInfo[],
): Promise<void> {
  const entries = await fs.readdir(dirPath, { withFileTypes: true });

  for (const entry of entries) {
    const fullPath = path.join(dirPath, entry.name);

    if (isHidden(entry.name, settings)) continue;

    if (entry.isDirectory()) {
      if (!isStaticFolder(entry.name, settings)) {
        await collectPageInfos(fullPath, rootDir, settings, pageInfos);
      }
    } else if (entry.isFile()) {
      const ext = path.extname(entry.name).toLowerCase();
      if (isBlocked(ext, settings)) continue;

      if (ext === ".md" || (ext === ".mdx" && settings.mdx.enabled)) {
        const content = await fs.readFile(fullPath, "utf-8");
        const baseName = entry.name.replace(/\.(md|mdx)$/, "");
        const baseNameLower = baseName.toLowerCase();

        let urlPath: string;
        if (baseNameLower === "readme") {
          let rawUrlPath =
            "/" + path.relative(rootDir, dirPath).replace(/\\/g, "/");
          if (rawUrlPath !== "/") rawUrlPath += "/";
          urlPath = rawUrlPath;
        } else {
          urlPath =
            "/" +
            path
              .relative(rootDir, fullPath)
              .replace(/\\/g, "/")
              .replace(/\.(md|mdx)$/, "");
        }

        // Quick frontmatter parse (không cần full render)
        const { renderMarkdown } = await import("../renderer/markdown.js");
        const result = renderMarkdown(content, settings);
        const title =
          (result.frontmatter.title as string) ||
          extractFirstHeading(content) ||
          humanizeFilename(baseName);

        pageInfos.push({
          urlPath,
          outputPath: "", // không cần outputPath cho OG-only
          title,
          description: result.frontmatter.description as string | undefined,
        });
      }
    }
  }
}
```

---

## 🎯 Task 3: Cache Clearing Functionality

### Mục đích

Thêm khả năng xóa cache static và OG để buộc render lại toàn bộ:

- `mdsvr ./docs --clear-cache` – xóa `_html/public/` directory (cache static + OG)
- `mdsvr ./docs -e -f` – force clear cache trước khi export

### Files affected

- 📝 **Modify:** `src/cli.ts` – thêm flags và logic cache clearing

### Thêm flags vào `ParsedArgs`

```typescript
interface ParsedArgs {
  // ...existing...
  clearCache: boolean;
  force: boolean;
}
```

### Thêm parsing logic

```typescript
// Trong parseArgs(), khởi tạo:
const args: ParsedArgs = {
  // ...existing...
  clearCache: false,
  force: false,
};

// Trong vòng lặp for, thêm:
} else if (arg === "--clear-cache") {
  args.clearCache = true;
} else if (arg === "-f") {
  args.force = true;
```

### Implement `clearCache()` function

```typescript
async function clearCache(
  dir: string,
  exportPath?: string,
  silent: boolean = false,
): Promise<void> {
  const { rm, access } = await import("node:fs/promises");

  const absDir = path.resolve(dir);

  // Default cache directories to clear
  const cacheDirs: string[] = [];

  // Add default static export directory (public folder only)
  const defaultStaticDir = path.join(absDir, "_html", "public");
  cacheDirs.push(defaultStaticDir);

  // Add custom export directory if specified
  if (exportPath) {
    const absExportPath = path.resolve(exportPath);
    if (!cacheDirs.includes(absExportPath)) {
      cacheDirs.push(absExportPath);
    }
  }

  let clearedCount = 0;

  for (const cacheDir of cacheDirs) {
    try {
      await access(cacheDir);
      await rm(cacheDir, { recursive: true, force: true });
      clearedCount++;
      if (!silent) {
        const relativePath = path.relative(absDir, cacheDir);
        console.log(`  ✓ Cleared cache: ${relativePath || cacheDir}`);
      }
    } catch {
      // Directory doesn't exist, skip
    }
  }

  if (!silent && clearedCount > 0) {
    console.log(`\n  ✔ Cache cleared: ${clearedCount} directories removed\n`);
  } else if (!silent && clearedCount === 0) {
    console.log(`\n  ℹ No cache directories found to clear\n`);
  }
}
```

### Thêm handler trong `main()`

```typescript
// Handle --clear-cache
if (args.clearCache) {
  try {
    await clearCache(absoluteDir, undefined, args.silent);
    process.exit(0);
  } catch (err) {
    console.error(
      "Cache clear error:",
      err instanceof Error ? err.message : err,
    );
    process.exit(1);
  }
}
```

### Thêm force clear logic vào export handler

```typescript
// Handle --export
if (/* existing export condition */) {
  try {
    // Force clear cache if -e -f flags are used together
    if (args.force) {
      if (!args.silent) {
        console.log(`\n  🗑️  Force clearing cache before export...\n`);
      }
      const outputDir = args.export || path.join(absoluteDir, "_html", "public");
      await clearCache(absoluteDir, outputDir, args.silent);
    }

    // ...existing export logic...
  } catch (err) {
    // ...existing error handling...
  }
}
```

### Cập nhật `printHelp()`

```typescript
function printHelp(): void {
  console.log(`
Usage: mdsvr [dir] [options]

Options:
  [dir]              Root directory to serve (default: .)
  -p, --port N       Port number (default: 1800)
  --host H           Bind address (default: localhost)
  -o, --open         Auto-open browser
  -s, --silent       Suppress console output
  -e, --export [PATH]  Export static HTML (default: _html/public in input dir)
  -f                 Force clear cache before export (use with -e)
  --clear-cache      Clear static and OG cache directories
  --init             Create a starter settings.json in [dir]
  --validate         Validate settings.json and exit
  --no-watch         Disable settings.json hot-reload
  -v, --version      Print version
  -h, --help         Print this help

Examples:
  mdsvr ./docs
  mdsvr ./notes --port 4000 --open
  mdsvr . --host 0.0.0.0 --port 8080
  mdsvr ./docs --export       # Export to ./_html/public
  mdsvr ./docs -e ./output    # Export to custom directory
  mdsvr ./docs -e -f          # Export with force cache clear
  mdsvr ./docs --clear-cache  # Clear cache directories
  mdsvr ./docs --init
  mdsvr ./docs --validate
`);
}
```

### Edge Cases đã xử lý

- **Custom export folder**: `clearCache()` nhận `exportPath` parameter để xóa cả custom output directory
- **Missing directories**: `access()` check trước khi `rm()` – không báo lỗi nếu directory không tồn tại
- **Silent mode**: Respect `args.silent` flag để không output khi cần thiết
- **Relative paths**: Hiển thị relative paths trong console output cho dễ đọc

---

## 🎯 Task 4: Export `exportOgImages` từ `src/index.ts`

Thêm vào `src/index.ts`:

```typescript
export {
  exportStaticSite,
  exportOgImages,
  type ExportOptions,
  type ExportOgOptions,
} from "./generators/static-export.js";
```

---

## 🧪 Testing Checklist

```bash
# Build
npm run build

# 1. Export OG only (skip existing)
mdsvr ./docs -eo
# Expect: gen các ảnh OG chưa có

# 2. Export static only (không gen OG)
mdsvr ./docs -es
# Expect: chỉ có HTML files, không có public/og/ mới

# 3. Export full (HTML + OG)
mdsvr ./docs -e
# Expect: cả HTML lẫn OG images

# 4. Cache clearing
mdsvr ./docs --clear-cache
# Expect: xóa _html/ directory

# 5. Force clear with export
mdsvr ./docs -e -f
# Expect: clear cache trước, sau đó export lại từ đầu

# 6. Help text
mdsvr --help
# Expect: thấy -eo, -es, -e, -f, --clear-cache
```

---

## 📊 Implementation Summary

| Task      | File                              | Changes                                                   | LOC ước tính |
| --------- | --------------------------------- | --------------------------------------------------------- | ------------ |
| 1         | `src/cli.ts`                      | Thêm 2 flags + 2 handlers                                 | ~60          |
| 2         | `src/generators/static-export.ts` | `exportOgImages()`, `collectPageInfos()`, `skipOg` option | ~100         |
| 3         | `src/cli.ts`                      | Cache clearing flags + logic                              | ~60          |
| 4         | `src/index.ts`                    | Export mới                                                | ~3           |
| **Total** |                                   |                                                           | **~223**     |

---

## ✅ Success Criteria

- ✓ `mdsvr . -eo` chỉ gen OG, skip existing
- ✓ `mdsvr . -es` chỉ export HTML, không đụng OG
- ✓ `mdsvr . -e` export cả 2 (không thay đổi hành vi hiện tại)
- ✓ `mdsvr ./docs --clear-cache` xóa `_html/public/` directory (cache static + OG)
- ✓ `mdsvr ./docs -e -f` force clear cache trước khi export
- ✓ Help text cập nhật đúng 5 flags (-eo, -es, -e, -f, --clear-cache)
- ✓ OG gen không làm chậm quá trình HTML export khi dùng `-es`
- ✓ Edge cases: custom export folder, missing directories, silent mode

---

## ⚠️ Edge Cases

- **OG disabled** (`settings.seo.og.enabled = false`): `-eo` in warning và exit
- **Font download timeout**: đã có retry/cache trong `src/og/generator.ts` – không cần thêm
- **`basePath` setting**: `getOgImagePath()` đã handle `basePath` – dùng lại như hiện tại
- **Cache clearing edge cases**:
  - **Custom export folder**: `clearCache()` nhận `exportPath` parameter để xóa cả custom output directory
  - **Missing directories**: `access()` check trước khi `rm()` – không báo lỗi nếu directory không tồn tại
  - **Silent mode**: Respect `args.silent` flag để không output khi cần thiết
  - **Relative paths**: Hiển thị relative paths trong console output cho dễ đọc

---

**Ready for Code Agent! 🚀**
