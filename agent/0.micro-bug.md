# Micro Bugs

## Sidebar

- [ ] **Folder icon alignment**: Folder có sub-items hiển thị collapse arrow (▶) lệch alignment so với folder không có sub-items (chỉ có icon 📁). Fix: bỏ collapse arrow, thay bằng emoji icon — closed folder: 📁, open folder: 📂

- [ ] **Persist sidebar hide/show vào localStorage**: Trạng thái ẩn/hiện sidebar chưa được lưu — sau khi reload trang sẽ reset về mặc định. Cần lưu vào `localStorage` và restore khi load trang. **Lưu ý:** chỉ áp dụng cho sidebar desktop; mobile float sidebar giữ nguyên logic cũ

## Code Block

- [ ] **Copy code button**: Code block chưa có nút copy — cần thêm button "Copy" vào góc trên phải của mỗi code block, click sẽ copy nội dung vào clipboard

## Mermaid

- [ ] **Mermaid zoom/fullscreen bị pixelated**: Khi zoom hoặc mở fullscreen, chart bị vỡ kiểu raster (như JPG) thay vì scale mượt như SVG — có thể Mermaid đang được render thành ảnh bitmap thay vì giữ nguyên SVG
- [ ] **Mermaid expand mode**: Thêm mode expand chiếm toàn bộ vùng `main.content` (không phải fullscreen browser) — toggle button để phóng to chart trong layout, giữ sidebar/header vẫn hiển thị

## Directory Page

- [ ] **Title xấu khi dir không có README**: Page title hiển thị dạng `index of /dir` — cần lấy title theo logic sidebar (parse từ tên folder, bỏ prefix số thứ tự, capitalize)
- [ ] **Breadcrumb name không thống nhất với sidebar**: Tên hiển thị trong breadcrumb cần dùng cùng logic parse với sidebar (bỏ prefix số, format tên) thay vì dùng raw folder name

## Export HTML → Serve

- [ ] **Root OG tag thiếu**: Root render README đã có `.jpg` OG image được generate, nhưng HTML thiếu `<meta>` tag trỏ tới nó
- [ ] **Dir không có README — OG image không hiển thị**: Dir không có README thì OG `.jpg` không thấy (dù link có thể đã có trong HTML) — chưa test kỹ trường hợp này
- [ ] **Font tiếng Việt lỗi khi render OG image**: Ký tự tiếng Việt (dấu thanh, dấu mũ) bị lỗi/không hiển thị đúng trong OG image — cần load font hỗ trợ Unicode/Vietnamese khi generate ảnh
