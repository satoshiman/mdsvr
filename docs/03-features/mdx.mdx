---
title: MDX Components
description: Interactive components for your documentation
---

# MDX Components

mdsvr supports **MDX** — Markdown with JSX. This allows you to use interactive components directly in your documentation.

<Callout type="info">
  MDX must be enabled in `_mdsvr/settings.json` with `"mdx.enabled": true`.
</Callout>

## Callout

Use callouts to highlight important information:

```mdx
<Callout type="info">This is an informational callout.</Callout>

<Callout type="warning" title="Important">
  This will delete all your data.
</Callout>
```

### Types

<Callout type="info">**Info** — General information and tips</Callout>

<Callout type="warning">**Warning** — Caution about potential issues</Callout>

<Callout type="danger">
  **Danger** — Critical warnings about destructive actions
</Callout>

<Callout type="success">
  **Success** — Positive confirmation or success states
</Callout>

<Callout type="tip">**Tip** — Helpful suggestions and best practices</Callout>

## Steps

Create step-by-step guides:

```mdx
<Steps>
  ### Step 1: Install
  Run `npm install mdsvr`

### Step 2: Configure

Create your `_mdsvr/settings.json`

### Step 3: Launch

Run `npx mdsvr ./docs`

</Steps>
```

<Steps>
  ### Install the package
  Run `npm install mdsvr`

### Create your docs folder

Create a `./docs` directory with some `.md` files

### Start the server

Run `npx mdsvr ./docs --open`

</Steps>

## Cards

Display links as beautiful cards:

```mdx
<Card title="Quick Start" icon="⚡" href="/quickstart">
  Get up and running in 5 minutes
</Card>
```

<Card title="Configuration" icon="⚙️" href="/configuration">
  Full settings reference
</Card>

### Card Groups

<CardGroup cols={2}>
  <Card title="Markdown" icon="📝" href="#">
    Standard Markdown support
  </Card>
  <Card title="MDX" icon="⚛️" href="#">
    React components in docs
  </Card>
  <Card title="Themes" icon="🎨" href="#">
    Dark and light mode
  </Card>
  <Card title="Search" icon="🔍" href="#">
    Full-text search
  </Card>
</CardGroup>

## Tabs

Organize content in tabs:

````mdx
<Tabs items={["npm", "yarn", "pnpm"]}>
  <Tab>```bash npm install mdsvr ```</Tab>
  <Tab>```bash yarn add mdsvr ```</Tab>
  <Tab>```bash pnpm add mdsvr ```</Tab>
</Tabs>
````

## Code Groups

Group related code blocks:

````mdx
<CodeGroup title="Installation">
  ```bash npm npm install mdsvr ``` ```bash yarn yarn add mdsvr ``` ```bash pnpm
  pnpm add mdsvr ```
</CodeGroup>
````

<CodeGroup title="Configuration">

```json settings.json
{
  "site": {
    "title": "My Docs"
  }
}
```

```js mdsvr.config.js
module.exports = {
  site: {
    title: "My Docs",
  },
};
```

</CodeGroup>

## Accordion

Create collapsible sections:

```mdx
<Accordion title="Is this free?">
  Yes, mdsvr is open source under MIT license.
</Accordion>
```

<Accordion title="What are the system requirements?">
  Node.js 18+ is required to run mdsvr.
</Accordion>

<Accordion title="Can I use custom components?">
  Built-in components are available. Custom components coming in v3!
</Accordion>

## Badge

Add status badges inline:

```mdx
New feature <Badge color="green">New</Badge>

Beta feature <Badge color="orange">Beta</Badge>

Deprecated <Badge color="red">Deprecated</Badge>
```

Available colors: `green`, `orange`, `red`, `blue`, `purple`, `gray`

- <Badge color="green">New</Badge> — New features
- <Badge color="orange">Beta</Badge> — Beta features
- <Badge color="red">Deprecated</Badge> — Deprecated features
- <Badge color="blue">Info</Badge> — Informational
- <Badge color="purple">Tip</Badge> — Tips and tricks
- <Badge color="gray">Note</Badge> — General notes

## Mermaid

Create diagrams with Mermaid syntax:

```mdx
<Mermaid>
  graph TD
    A[Start] --> B{Is it working?}
    B -->|Yes| C[Great!]
    B -->|No| D[Debug]
    D --> E[Fix Issues]
    E --> B
</Mermaid>
```

<Callout type="tip">
  Mermaid diagrams are rendered client-side, so they work without any
  server-side dependencies.
</Callout>

## Frontmatter

All MDX files support YAML frontmatter for metadata:

```mdx
---
title: Page Title
description: Page description for SEO
image: ./assets/og-image.png
date: 2025-01-15
author: John Doe
tags: [setup, quickstart]
noIndex: false
---

# Your Content
```

This metadata is used for:

- SEO `<title>` and meta tags
- Open Graph / Twitter Cards
- RSS feed entries (when `date` is present)
- Search index weighting

## MDX Plugins

You can add custom remark and rehype plugins to extend MDX functionality:

```json
{
  "mdx": {
    "enabled": true,
    "remarkPlugins": ["remark-gfm"],
    "rehypePlugins": ["rehype-slug"]
  }
}
```

**Options:**

- `remarkPlugins`: Array of remark plugin names for markdown transformations
- `rehypePlugins`: Array of rehype plugin names for HTML transformations

Currently, mdsvr includes these built-in plugins by default:

- `remark-frontmatter` - Parse YAML frontmatter
- `remark-gfm` - GitHub Flavored Markdown support
- `remark-mdx-frontmatter` - MDX frontmatter support
- `rehype-slug` - Generate heading IDs for anchors
- `rehype-autolink-headings` - Auto-link headings

You can add additional plugins by installing them and adding their names to the arrays.
